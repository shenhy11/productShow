# Zioncom 项目前后端打通与深化实施待办清单 (TODO List)

该文档用于记录和追踪 Zioncom 企业官网项目从目前的“基础毛坯 + 模拟前台”状态，跨越到“全栈真实动态数据运作”的剩余关键任务。

## 阶段一：让后台能够“维护数据” (Admin 管理端建设) —— **【待办】**
*当前进度：已准备好底层 SQL 数据库表结构，但在 RuoYi 若依前端中缺乏对应操作可视界面。*

- [ ] **1.1. 代码生成：** 启动若依的逆向工程服务，依据 `zioncom_product`、`zioncom_content`、`zioncom_support`、`zioncom_brand` 数据库表快速生成增删改查前端页面和后端 Controller/Service 层代码。
- [ ] **1.2. 页面嵌入：** 将生成的 `.vue` 页面组件正式集成入 `zioncom-admin/src/views/` 目录下。
- [ ] **1.3. 建立权限路由：** 在若依系统的“菜单管理”中新建一套独立的一级菜单【🌐 Zioncom 业务管理】，并将产品目录、新闻咨询、支持体系等挂载到下方并授权给管理员角色。
- [ ] **1.4. 富文本与文件支持：** 在后台页面组件中调试图片与文件的上传逻辑，确保未来从 Zioncom 官网上扒下来的图片能正常存入服务器，并在表中映射真实保存记录。

## 阶段二：让前台能够“取到数据” (Nuxt 门店业务逻辑换血) —— **【待办】**
*当前进度：前台产品、首页和业务逻辑暂由 `/mock/` 中的静态 JSON 文件提供数据伪造支持。*

- [ ] **2.1. 编写对公开放 API：** 在 `zioncom-server` 中，屏蔽或绕过部分若依系统的原生鉴权网关（Spring Security），为前端前台撰写一些“完全开放（无需 Token，不设栏栅）”的查询接口，如：`/api/product/public/list` 等。
- [ ] **2.2. Nuxt 发动机改造 (移除 Mock 数据)：** 拆除 Nuxt 项目里的静态 JSON 请求；替换为 `$fetch` 直连对应真正的后端 `spring-api:8080` 服务提供数据的状态。
- [ ] **2.3. 前台数据组件联调验证：** 验证产品筛选条件（比如输入不同种类）、支持文档搜索等，确保从 MySQL -> Spring -> Nuxt Server -> 客户端的数据呈现链路完全成型。

## 阶段三：真实源数据对接与验收测试 —— **【待办】**
- [ ] **3.1. 源官网素材入库：** 根据最初确认需求，针对线上的 `https://www.zioncom.net/` 产品进行手动、或者半自动化爬取产品和支持记录，借助之前完善的【若依后台管理系统】将其填充满成百上千条真实数据。
- [ ] **3.2. 前台细节修复或 UI 升级：** 根据取得真实数据后前台展现效果的不匹配，重新适配图片高宽比和分页逻辑等样式问题。
- [ ] **3.3. 最终多机网网络验收发布。**
