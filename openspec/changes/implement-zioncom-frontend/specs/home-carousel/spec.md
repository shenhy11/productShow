## 新增需求 (ADDED Requirements)

### 需求: 首页主视觉轮播模块 (Hero Carousel Module)
首页顶部**必须**包含一个全宽的高质量产品轮播图，实现至少 3-5 个核心推荐产品型号（如搭载 Wi-Fi 6 的主推外置路由或网卡）的平滑自动切换与焦点显示。

#### 场景: 用户浏览首屏主页 (Viewing the home page)
- **当 (WHEN)** 访客直达站点根路径 `/` 时
- **那么 (THEN)** 这个包含图片与极简标语的轮播器需要占满视觉首位框并不停留超过限定时间自动切换焦点图页。
